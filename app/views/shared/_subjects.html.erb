<% unless metadata[:subjects].empty? %>
    <dd>Subjects</dd>
    <% metadata[:subjects].each do |subject| %>
        <dt><%= link_to((raw subject), "/catalog?f[subject_sim][]="+ (raw subject)) %></dt>
    <% end %>
<% end %>
